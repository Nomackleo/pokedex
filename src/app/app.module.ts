/**
 * @deprecated AppModule has been replaced by the standalone bootstrap flow in Angular 20.
 *             The file is kept temporarily to avoid breaking tooling that still references it.
 */
export {};
